#include <bits/stdc++.h>

using namespace std;

using ll = long long;

void solve() {
    int N, M;
    cin >> N >> M;

    vector<ll> A(N);
    for (int i = 0; i < N; i++) cin >> A[i];

    vector<ll> B(N-1);
    for (int i = 0; i < N-1; i++) cin >> B[i];

    for (int i = 0; i < N-1; i++) {}
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();

    return 0;
}